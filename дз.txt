# создать 4 функции (select, update, insert, delete)
# и реализовать механнику работы с базой через них

# если введена строка, то добавляем в поле имя
# если имя уже было, то меняем его на введенное
# если введено число, то удаляем данную строчку по id или пишем что такого id нет